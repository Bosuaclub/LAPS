# Minimal requirements for Vercel deployment
# Only includes essentials to stay under size limits

fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Core FAO-Sim (lightweight)
numpy>=1.24.0
pandas>=2.0.0

# Note: Full ML dependencies (torch, timesfm, langchain)
# are too large for Vercel. Use a dedicated backend service
# (Railway, Render, etc.) for full functionality.
